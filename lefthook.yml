pre-commit:
  jobs:
    - name: biome
      glob: "*.{js,css,html,json}"
      run: pnpm exec biome check --write {staged_files}
      stage_fixed: true

    - name: check
      glob: "*.js"
      run: node --check {staged_files}
